# Mobile Auth Broker Configuration
# Copy this file to .env and fill in the values

# Required: GitHub OAuth App Client ID
GITHUB_CLIENT_ID=your-github-oauth-app-client-id

# Required: Server secret for token hashing (must be at least 32 bytes)
SERVER_SECRET=your-64-byte-random-secret-here

# Required: Broker hostname
BROKER_HOSTNAME=mobile.claw.example.org

# Required: Gateway service URL (in-cluster)
GATEWAY_SERVICE_URL=http://sctg-claw:18789

# Required: Comma-separated list of allowed emails
ALLOWED_EMAILS=user1@example.com,user2@example.com

# Optional: Token lifetimes
ACCESS_TOKEN_TTL=1h
REFRESH_TOKEN_TTL=720h

# Optional: Listen address
LISTEN_ADDR=:8080

# Optional: Comma-separated list of ports to listen on instead of LISTEN_ADDR
# (all serve the same handler), e.g. LISTEN_PORTS=80,8080,18789
# LISTEN_PORTS=

# Optional: Database path
DATABASE_PATH=/data/broker.db

# Optional: Maximum WebSocket message size (16MB default)
MAX_MESSAGE_SIZE=16777216

# Optional: Poll interval scale factor
POLL_INTERVAL_SCALE=1.5

# Optional: Self-managed TLS via ACME DNS-01 against Cloudflare -- see
# README.md#self-managed-tls. Required native clients (ws:///http:// gateway
# URLs are rejected client-side). All TLS_* below are required together once
# TLS_ENABLED=true.
# TLS_ENABLED=true
# TLS_PORTS=443
# TLS_DOMAIN=mobile-claw-broker.your-tailnet.example.ts.net
# TLS_ACME_EMAIL=you@example.com
# TLS_CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-scoped-to-dns-edit
# TLS_CERT_CACHE_DIR=/data/certs
# TLS_ACME_STAGING=false
