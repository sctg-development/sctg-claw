{{- if .Values.openclaw.secret.create }}
{{- if .Values.openclaw.existingSecret }}
{{- fail "openclaw.existingSecret cannot be set when openclaw.secret.create is true" }}
{{- end }}
{{- if not (or .Values.openclaw.secret.gatewayToken .Values.openclaw.secret.gatewayPassword .Values.openclaw.secret.mistralApiKeys .Values.openclaw.secret.cohereApiKeys .Values.openclaw.secret.poolsideApiKeys) }}
{{- fail "set a gateway token or at least one provider key when openclaw.secret.create is true" }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "claw.openclawSecretName" . }}
  labels:
    {{- include "claw.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- with .Values.openclaw.secret.gatewayToken }}
  OPENCLAW_GATEWAY_TOKEN: {{ . | quote }}
  {{- end }}
  {{- with .Values.openclaw.secret.gatewayPassword }}
  OPENCLAW_GATEWAY_PASSWORD: {{ . | quote }}
  {{- end }}
  {{- with .Values.openclaw.secret.mistralApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "MISTRAL" "raw" .) | nindent 2 }}
  {{- end }}
  {{- with .Values.openclaw.secret.cohereApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "COHERE" "raw" .) | nindent 2 }}
  {{- end }}
  {{- with .Values.openclaw.secret.poolsideApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "POOLSIDE" "raw" .) | nindent 2 }}
  {{- end }}
  {{- with .Values.openclaw.secret.exaApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "EXA" "raw" .) | nindent 2 }}
  {{- end }}
  {{- with .Values.openclaw.secret.firecrawlApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "FIRECRAWL" "raw" .) | nindent 2 }}
  {{- end }}
  {{- with .Values.openclaw.secret.googlePlacesApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "GOOGLE_PLACES" "raw" .) | nindent 2 }}
  {{- end   }}
  {{- with .Values.openclaw.secret.elevenlabsApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "ELEVENLABS" "raw" .) | nindent 2 }}
  {{- end   }}
  {{- with .Values.openclaw.secret.openaiApiKeys }}
  {{- include "claw.providerApiKeyEnv" (dict "envName" "OPENAI" "raw" .) | nindent 2 }}
  {{- end   }}
  {{- with .Values.openclaw.secret.openaiBaseUrl }}
  OPENAI_BASE_URL: {{ . | quote }}
  {{- end }}
  {{- with .Values.openclaw.secret.ghToken }}
  GITHUB_TOKEN: {{ . | quote }}
  {{- end }}
  {{- with .Values.openclaw.secret.onePasswordServiceAccountToken }}
  OP_SERVICE_ACCOUNT_TOKEN: {{ . | quote }}
  {{- end }}
  {{- with .Values.openclaw.secret.keypoolVaultUrl }}
  KEYPOOL_VAULT_URL: {{ . | quote }}
  {{- end }}
  {{- with .Values.openclaw.secret.keypoolLiveSecret }}
  KEYPOOL_LIVE_SECRET: {{ . | quote }}
  {{- end }}
{{- end }}